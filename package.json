{
  "name": "restaurant-page",
  "version": "1.0.0",
  "description": "A restaurant page that is rendered solely using Javascript (besides the HTML skeleton). Webpack is used to bundle this project's scripts and sources.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "css-loader": "^6.9.0",
    "style-loader": "^3.3.4",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  }
}
